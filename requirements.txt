# Python >= 3.10 (uses dataclasses + from __future__ import annotations heavily).
# Setup (mirrors .github/workflows/ci.yml, CPU-only):
#   python3 -m venv .venv && . .venv/bin/activate
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
#   pip install -r requirements.txt
#   python -m pytest tests/ -q     # expected: 24 passed
numpy
torch
pyyaml
pytest
matplotlib   # only needed to regenerate the paper figures (paper/make_figs.py)
